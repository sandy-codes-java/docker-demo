spring:
  datasource:
    url: jdbc:mysql://mysql:3306/assignment_schema
    username: root
    password: root
  application:
    name: docker-demo
  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect
      ddl-auto: update